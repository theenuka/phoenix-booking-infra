# --- Terraform ---
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
.terraform.lock.hcl
override.tf
override.tf.json

# --- Ansible ---
*.retry
inventory.ini  # Optional: block this if you don't want IPs on GitHub, but okay for now.

# --- OS Files ---
.DS_Store
Thumbs.db

# --- Secrets (Just in case you accidentally put them here) ---
*.pem
*.key
.env